<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.springStarter.dao.BoardDao">
    <sql id="searchCondition">
        <choose>
            <when test='option eq "T"'>
                title like concat('%', #{keyword}, '%')
            </when>
            <when test='option eq "W"'>
                writer like concat('%', #{keyword}, '%')
            </when>
            <otherwise>
                title like concat('%', #{keyword}, '%') or
                content like concat('%', #{keyword}, '%')
            </otherwise>
        </choose>
    </sql>

    <sql id="selectFromBoard">
        select bno, title, content, writer, viewCnt, commentCnt, regDate, modiDate from board
    </sql>

    <select id="searchSelectPage" parameterType="searchCondition" resultType="Board">
        <include refid="selectFromBoard"/>
        where <include refid="searchCondition"/>
        order by bno desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="searchResultCnt" parameterType="searchCondition" resultType="int">
        select count(*) from board
        where <include refid="searchCondition"/>
    </select>

    <select id="count">
        select count(*) from board
    </select>

    <select id="select" resultType="Board" parameterType="int">
        <include refid="selectFromBoard"/>
        where bno = #{bno}
    </select>

    <select id="selectPage" parameterType="map" resultType="Board">
        <include refid="selectFromBoard"/>
        order by bno desc limit #{offset}, #{pageSize}
    </select>

    <select id="selectAll" resultType="Board">
        <include refid="selectFromBoard"/>
        order by bno desc
    </select>

    <insert id="insert" parameterType="Board">
        insert into board(title, content, writer) values(#{title}, #{content}, #{writer})
    </insert>

    <update id="update" parameterType="Board">
        update board set title = #{title}, content = #{content}, modiDate = now() where writer = #{writer} and bno = #{bno}
    </update>

    <update id="updateViewCnt" parameterType="int">
        update board set viewCnt = viewCnt + 1 where bno = #{bno}
    </update>

    <delete id="delete" parameterType="map">
        delete from board where writer = #{writer} and bno = #{bno}
    </delete>

    <delete id="deleteAll">
        delete from board
    </delete>
</mapper>