<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.springStarter.dao.CommentDao">
    <sql id="selectFromComment">
        select cno, bno, comment, commenter, reg_date, up_date from comment
    </sql>

    <insert id="insert" parameterType="Comment">
        insert into comment(bno, comment, commenter) values(#{bno}, #{comment}, #{commenter})
    </insert>

    <update id="update" parameterType="Comment">
        update comment set comment = #{comment}, up_date = now() where cno = #{cno} and commenter = #{commenter}
    </update>

    <select id="selectAll" parameterType="int" resultType="Comment">
        <include refid="selectFromComment"/> where bno = #{bno} order by cno asc
    </select>

    <select id="select" parameterType="int" resultType="Comment">
        <include refid="selectFromComment"/> where cno = #{cno}
    </select>

    <delete id="delete" parameterType="map">
        delete from comment where cno = #{cno} and commenter = #{commenter}
    </delete>

    <delete id="deleteAll" parameterType="int">
        delete from comment where bno = #{bno}
    </delete>

    <select id="count" parameterType="int" resultType="int">
        select count(*) from comment where bno = #{bno}
    </select>
</mapper>